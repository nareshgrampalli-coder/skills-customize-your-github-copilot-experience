from sklearn.datasets import load_iris
from sklearn.model_selection import train_test_split
from sklearn.neighbors import KNeighborsClassifier
from sklearn.metrics import accuracy_score, classification_report

# TODO: Task 1 — Load the Iris dataset
# iris = load_iris()
# X = iris.data
# y = iris.target

# TODO: Task 1 — Print the number of samples and features


# TODO: Task 1 — Print the feature names and class names


# TODO: Task 1 — Print the first 5 rows of feature data with their labels


# TODO: Task 2 — Split data into 80% training and 20% test sets
# Use train_test_split with random_state=42
# X_train, X_test, y_train, y_test = ...


# TODO: Task 2 — Print the number of training and test samples


# TODO: Task 2 — Create a KNeighborsClassifier with n_neighbors=3 and train it
# model = KNeighborsClassifier(n_neighbors=3)
# model.fit(...)


# TODO: Task 3 — Predict class labels for the test set
# y_pred = model.predict(X_test)


# TODO: Task 3 — Calculate and print the accuracy score as a percentage


# TODO: Task 3 — Print the full classification report using classification_report
# Use target_names=iris.target_names


# TODO: Task 3 — Predict the class for a single new sample [5.1, 3.5, 1.4, 0.2]
# and print the class name
